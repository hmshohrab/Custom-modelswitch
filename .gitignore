# Backups written by the config writers
*.backup-*

# Editor / OS noise
.vscode/
.idea/
*.swp
Thumbs.db
.DS_Store

# Local environment variables containing API keys
.env
.env.local
*.env

# Local, user-specific preset overrides (optional convention)
AI-Config-Presets.local.json

# Claude Code memory (local only, not for the repo)
CLAUDE.md
